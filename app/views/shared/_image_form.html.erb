<% if logged_in? %>
  <%= form_for(@image) do |f| %>
    <%= f.text_field :content %>
    <%= f.file_field :imager %>

    <%= f.label :private %>
    <%= f.check_box :private %>
    <%= f.submit "New Picture"%>
  <% end %>
<% end %>